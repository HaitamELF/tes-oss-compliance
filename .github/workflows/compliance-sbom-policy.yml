name: compliance-sbom-policy

on:
  pull_request:
  push:
    branches: [main]
  schedule:
    - cron: "0 3 * * *"

jobs:
  sbom-policy:
    uses: org/oss-compliance/.github/workflows/reusable-sbom-policy.yml@v1
    with:
      upload_sbom: true